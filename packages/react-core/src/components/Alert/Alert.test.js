import React from 'react';
import { shallow } from 'enzyme';
import Alert from './Alert';

const props = {};

test('replace with useful test name', () => {
  const view = shallow(<Alert {...props} />);
  // Add a useful assertion here.
  expect(view).toBe(false);
});
